#ifndef CIRCLES_H
#define CIRCLES_H
#include <SDL3/SDL.h>

#endif //CIRCLES_H

void drawCircle(SDL_Renderer* renderer, float centerX, float centerY, float radius);
void drawCircleWidth(SDL_Renderer* renderer, float centerX, float centerY, float radius, float width);
void drawFilledCircle(SDL_Renderer *renderer, float centerX, float centerY, float radius);
